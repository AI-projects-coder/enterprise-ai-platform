DATABASE_URL=postgresql+asyncpg://platform:platform@postgres:5432/platform
JWT_SECRET=dev-secret-change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Real secret — get one from https://aistudio.google.com/apikey and put it in
# apps/api/.env (gitignored). Never commit a real key here.
GEMINI_API_KEY=
